doctype html
html(lang="ru")
    head
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible" content="ie=edge")
        meta(http-equiv="Content-Type" content="text/html; charset=UTF-8")

        meta(name="description" content="description")
        meta(name="keywords" content="keywords")
        title Analog Searching Demo

        link(rel="icon" type="image/png" href="data:image/gif;base64,R0lGODdhAQABAPAAAMPDwwAAACwAAAAAAQABAAACAkQBADs=")

        link(rel='preload', href='css/main.css', as='style')
        link(rel='stylesheet', href='css/main.css')

        if process.env.NODE_ENV === 'dev'
            link(rel='preload', href='css/main.css', as='style')
            link(rel='stylesheet', href='css/main.css')
        else
            link(rel='preload', href='css/main.min.css', as='style')
            link(rel='stylesheet', href='css/main.min.css')

    // style не переносим!
    style.
        .container {
            width: 100%;
            max-width: 1400px;
            margin: 0 auto;
        }

        html, body {
            margin: 20px 0;
            padding: 0;
        }

    body
        .container
            .asd
                .asd__wrap
                    h1.asd__title Попробовать поиск товара по фотографии

                    .asd__tubs-columns
                        button(type="button" data-target-selector="#searchForm").active Указать параметы
                        button(type="button" data-target-selector="#feedForm") Добавить свою выгрузку

                    .asd__tubs-types#tubsTypes
                        button.active(type="button" data-type="code") Поиск по артикулу
                        button(type="button" data-type="url") Поиск по url
                        button(type="button" data-type="image") Поиск по фото

                    .asd__container
                        .asd__column.asd__column_toggle#searchForm.active
                            include components/search-form/search-form

                        .asd__column.asd__column_toggle#feedForm
                            include components/feed-form/feed-form

                    .asd__container
                        .asd__column#examples
                            include components/examples/examples

                        .asd__column#warning
                            include components/warning/warning
                            include components/uploading/uploading

                .asd__wrap#results.hidden
                    include components/res-msg/res-msg
                    include components/results/results

                .modal.hidden#asdModal
                    .modal__body
                        h3.modal__title
                        p.modal__text
                        span.modal__line
                        button.modal__close(type="button") Закрыть

        if process.env.NODE_ENV === 'dev'
            script(src='js/main.js' defer)
        else
            script(src='js/main.min.js' defer)
